# 运行与发布

> 状态：1.0 方向已确认；本文不创建部署配置。关联决策：D016、D019、D021。1.0 不执行部署，除非项目所有者明确授权。

## 环境与发布（1.0 最小）

- 1.0 优先本地与测试环境跑通闭环；生产部署需项目所有者明确授权。
- 前端、API 和 Worker 使用独立发布单元，共享同一兼容性矩阵。
- 数据存储技术待定（D016）；部署拓扑随之确定。
- 模型路由和提示词采用配置发布、灰度验证和快速回退。

## SLO 基线（1.0 目标，非承诺）

- 提交改写 P95：尽快返回任务标识，不阻塞前端。
- AI 推演：显示阶段性进度；超时可安全重试或降级。
- 已确认事件持久性：数据库确认后不得因 Worker 或前端故障丢失。
- 具体 SLO 数值随实现阶段与 D016 一并确定。

## 可观测性（1.0 最小）

- 指标：请求延迟、错误率、队列深度、任务时长、重试、模型 Token/成本、状态写回失败、事件追加失败。
- 日志：结构化并带 request、session、turn、model-run 关联 ID；默认脱敏。
- 告警：队列阻塞、状态写回失败、事件追加失败、成本异常。
- 详细 SLO 燃尽与 Trace 覆盖随实现阶段补充。

## 容量与成本（1.0 最小）

- API 保持无状态，可水平扩展。
- Worker 按队列深度扩展；1.0 单会话顺序处理，避免同会话并行写入。
- 模型调用设置单次与会话预算。
- 具体托管服务与规模随 D016 与部署授权确定。

## 数据保留与恢复（1.0 最小）

- 原始模型会话与审计元数据保留策略待定（D019）。
- 事件日志为不可变权威记录，应纳入备份。
- 恢复目标与备份策略随 D016 与部署授权确定。

## 待定项

- 数据存储技术与部署拓扑（D016）。
- 具体 SLO 数值与恢复目标。
- 数据保留策略（D019）。
